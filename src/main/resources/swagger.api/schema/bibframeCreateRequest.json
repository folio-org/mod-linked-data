{
  "type": "object",
  "$schema": "http://json-schema.org/draft-04/schema#",
  "description": "Request body to create Bibframe record",
  "properties": {
    "graphName": {
      "type": "string",
      "description": "The name of the graph associated with the Bibframe object."
    },
    "graphHash": {
      "type": "integer",
      "description": "The hash value of the graph associated with the Bibframe object."
    },
    "slug": {
      "type": "string",
      "description": "The unique text identifier."
    },
    "configuration": {
      "type": "string",
      "description": "The configuration JSON for the Bibframe."
    }
  },
  "additionalProperties": false,
  "required": [ "resourceName" ]
}
