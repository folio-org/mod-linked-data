{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "description": "Reindexing Request Response",
  "type": "object",
  "properties": {
    "reindex": {
      "type": "boolean",
      "description": "If true reindexing starts, otherwise no action is taken"
    }
  }
}
